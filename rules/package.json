{
  "name": "@gamepark/its-a-wonderful-world",
  "version": "3.0.13",
  "description": "The rules of It's a Wonderful World adapted for Game Park",
  "sideEffects": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "Romain Fromi <romain@game-park.com> (https://game-park.com/)",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "vitest",
    "check:cycles": "madge --circular --extensions ts src",
    "build": "yarn check:cycles && tsc",
    "prepack": "yarn build",
    "publish": "yarn version ${0:-patch} && yarn npm publish && git commit -am v$(node -p \"require('./package.json').version\") && git tag v$(node -p \"require('./package.json').version\") && git push --follow-tags",
    "lint": "eslint --fix"
  },
  "peerDependencies": {
    "@gamepark/rules-api": ">=7",
    "es-toolkit": "^1.45.1"
  },
  "devDependencies": {
    "@gamepark/rules-api": "~7.6.4",
    "es-toolkit": "^1.49.0",
    "madge": "^8.0.0",
    "vitest": "^4.1.9"
  }
}
